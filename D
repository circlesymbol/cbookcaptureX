# Use an automated web-desktop image that establishes web routing instantly
FROM securecom/kasm-ubuntu-lxde:1.15.0

# Bind explicitly to Hugging Face's default communication port
ENV VNC_PORT=7860
EXPOSE 7860

# Redirect the environment variables to bypass root checks
ENV HOME=/tmp
WORKDIR /tmp
